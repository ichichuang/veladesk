{
  "name": "@veladesk/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Workspace, entity, page, dock and folder domain contracts for VelaDesk. Pure TypeScript, no persistence.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@veladesk/desktop-engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
